state = "menu"

while True:
	global state
	if state == "menu"
		codigo do menu aqui
		quando entra numa atividade
		state = nome da atividade

	if state == "bath"
		codigo do banho
		quando acaba o banho
		state = "menu"
	
	if state == "eat"
		codigo da comida
		quando acaba a comida
		state = "menu"

	if state == "sleep"
		codigo do sleep
		quando acaba o dormir
		state = "menu"

	if state == "fun"
		codigo do fun
		quando acaba o fun
		state == "menu"
